kernel   6.8.0-138-generic
logind   
sshd     OpenSSH_9.6p1 Ubuntu-3ubuntu13.19, OpenSSL 3.0.13 30 Jan 2024
stack    41 줄 (sshd)
date     2026-09-08T22:56:32+09:00

── 서비스 · 단계별 ──────────────────────────────────────────

  service      phase              건    scope 있음  proc=scope      대기 최대
  su-l         close_session      2    0/2         0/2               0
  su-l         open_session       2    0/2         0/2               0
  warrant-probe open_session       9    3/9         3/9               0

── 판정 ─────────────────────────────────────────────────────

  판정 제외 8건 — 호출자가 이미 사용자 세션 안이라
    새 세션이 만들어지지 않았다 (pam_systemd 가 생성을 건너뛴다).

  대상: warrant-probe (sshd 아님) · open_session 3건

  XDG_SESSION_ID 가 있다        3/3
  session-N.scope 가 이미 있다   3/3   ← §11 T1 의 가정
  /proc/self/cgroup 과 일치      3/3

  ✓ S3 통과. session 단계에서 scope 가 이미 확정돼 있다.
    pam_warrant.so 는 XDG_SESSION_ID → 경로 → stat 만으로 cgroup id 를
    얻을 수 있다. warrantd 의 cgroup 트리 순회는 필요 없다.

  ★ XDG_SESSION_ID 가 재사용됐다 — cgroup id 는 매번 다르다:
      session-4.scope  →  cgid 13299, 13408, 13517
    같은 세션 번호가 서로 다른 cgroup 을 가리켰다. 영장을 세션
    번호에 걸면 다음 세션이 남의 영장을 물려받는다.
    → cgroup id 를 키로 쓰는 §11 T1 설계가 옳다는 직접 증거다.

  (§11 T4) close_session 2건 중 scope 가 아직 살아 있음: 0
    살아 있으면 cgroup_warrant 엔트리 정리를 여기에 걸 수 있다.

  주의: sshd 기록이 없다. 지금까지 본 서비스 = {'warrant-probe': 9, 'su-l': 4}
    su 와 pamtester 는 기존 세션 안에서 불리면 새 세션을 만들지
    않으므로 §11 T1 을 잴 수 없다 — 모듈이 안 죽는다는 것만 확인된다.
    결론은 test-detached 또는 3단계(sshd)에서 나온다.
