package io.seswar.warrant.repository;

// @Repository
public interface UnwarrantedSessionRepository {

    // summarizeByNode(Instant from, Instant to)
    // summarizeByAccount(Instant from, Instant to)
    // 이 두 쿼리의 결과가 §07 STEP 2 의 리포트 한 장이 된다.
}
